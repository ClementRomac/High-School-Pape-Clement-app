<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.ynov.applpc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ParentsActivity" />
        <activity android:name=".StudentsActivity" />
        <activity android:name=".HighScoolActivity" />
        <activity android:name=".CdiActivity" />
        <activity android:name=".CvlActivity" />
        <activity android:name=".InfosActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".OrietationActivity"></activity>
    </application>

</manifest>
